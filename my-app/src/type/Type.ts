import React from "react";

export type ThreadsType = {
  id: string;
  title: string;
};

export type NewThreadsType = {
  threadId: string;
  title: string;
};

export type CommentType = { post: string };
